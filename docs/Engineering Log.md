---
tags:
  - engineering/log
  - ndt-suite
aliases:
  - Engineering Log
---

# Engineering Log

Use this page as the running handoff point between Obsidian notes and code work.

## Active Areas

- Vessel modeler coverage, piping, viewport rendering, and material handling.
- Industrial theme polish across admin, documents, personnel, projects, profile, demos, and layout styles.
- Clean design rollout: login page restyled 2026-07-16 (`src/pages/login.css`, `lg-` prefix) to match personnel/projects/profile.
- Email deliverability to Outlook/M365: code hardening extended 2026-07-16 to client senders + auth templates; committed (`ed0ab75`) and the 4 sender edge functions deployed. Auth templates still need re-pasting into the Supabase dashboard; corporate allow-list/quarantine check outstanding.
- Project inspection workflow, report readiness, scan viewer, and companion app integration.
- Supabase migrations, storage policies, and user/account security workflows.

## Recent Handovers

- [[plans/2026-08-06-t3-ux-batch-design]] - **ALL FIVE T3 PHASES LANDED 2026-08-06/07** (owner confirmed perf good after `50def17` first): A3 labeled undo + history dropdown (`2e4bd39` — HistoryEntry {vessel,label,at}, dispatcher-minted labels, folded UNDO_TO/REDO_TO jumps), C12 view cube/canonical views/camera bookmarks (`8685357` — canonical-views.ts, CSS-3D ViewCube rAF-synced, cameraBookmarks top-level field both paths + report-capture feed), C13a uniform entity visibility (`d9f5046` — visible? on all types, byte-identical hash projections, tier-2 in-place apply, raycast exclusion, stats untouched) + C13b outliner panel (`509bd62` — pure outliner-tree + OutlinerPanel, Eye toggles via D1 hooks), C14 command palette + **view-cube positioning fix** (Ctrl+K palette-registry/frame-entity/CommandPalette; the cube was mounted top-LEFT under the absolute 340px `.vm-sidebar` overlay and never visible — root-caused via Playwright drive, moved top-RIGHT, outliner made sidebar-aware; new viewport-overlay rule in Module Map), C15 clip planes (clip-planes.ts + scene-wide setClippingPlanes + rebuild re-application, ClipPlanesControl, runtime-verified GPU cut). Both C14 halves + C15 were runtime-verified in a headless-Edge Playwright drive (temp /verify-modeler route, reverted). Final gate: build ✓, suite 1549 passed | 3 skipped (useLayoutMode OOM flake only). Note: the four research subagents crashed on context overflow this arc — implementation agents ran fine with explicit chunked-read instructions in their prompts. Deferred: annotations/images visible→tier-2 unification (CSS2D label lifecycle), CSS2D labels don't clip, clipped geometry still raycastable.
- [[plans/2026-08-05-boot-parity-feedback-batch-design]] - **ALL FIVE ITEMS LANDED 2026-08-05/06** from the owner's hands-on review: R1 nozzle-bore stat cutouts (`050e0a1` — `engine/nozzle-footprint.ts`, radial exact / non-radial projected ellipse, per-body incl. boots, live memo-dep fix; deferred: head-mounted nozzles, boot-composite visual holes), R5 vertical-vessel 2D orientation (`50771d1` — FlattenedView never read `orientation`; `makeDevelopedFrame()` is now THE single (axial,circ)↔canvas mapping, vertical = pure transpose portrait, horizontal byte-identical), R3 user-facing "Boot" terminology (`9124a86` — strings only, identifiers/serialization untouched, persisted names keep their saved values), R4 relief view → **Topo viewport mode** (`2bab5fc` — 3D/2D/Topo stack, `ReliefViewportPane.tsx` overlay toolbar, `ReliefViewModal` deleted, viewMode transient/unserialized), R2 cursor-first cross-body mounting (`c098a15` — `engine/body-crossing.ts` 6mm seam hysteresis + frame-conversion invariant; drags/draws/drops raycast all bodies, Mount-on selects → `MountedOnChip`; excluded: dome scans, textures/saddles, pipe-part drops; confirmed composites have no gizmo → remount = re-enter orientation). Suite 1041→~1394. Next: T3 UX batch (C12 view cube/bookmarks, C13 outliner, C14 command palette, C15 clip planes).
- [[plans/2026-07-30-vessel-modeler-ux-roadmap]] - **Tranche 1 COMPLETE 2026-07-31** (`3d099a5` cutouts, `80401d1` per-body stats, `32f23db` unified sampler/A1, `1467e60` welds+lugs+rects, `b94c447` flattened footprints+strips, `7870af1` dome-on-end, `9a2d60b` annotations): full appendage feature parity — junction openings excluded from stats and clipped in overlays via one predicate; wall-loss/coverage per body with Combined default; every attachable mounts on appendages with spec-declared bodyId, cascade cleanup, byte-identical legacy goldens throughout. Punch list still open: weld-label placement on appendage welds; VesselModeler.tsx integrations deferred while the GA session holds the file (draw-on-appendage context, direct dome import to a closure, import-modal section label); 2D welds/lugs/dome-scans on appendage strips; if dome scans ever upsert to `scan_composites` keyed by section, disambiguate to `appendage:<id>:end`. Next tranche: T2 foundations (vessel-coords.ts, VesselModeler decomposition, stable nozzle ids).
- [[plans/2026-07-30-ga-drawing-import-hardening-design]] - IMPLEMENTED same day (all 5 phases incl. Addendum, uncommitted on `feature/appendage-bodies`). Addendum from BRT1-VA1 field test: **head-mounted nozzles** — extraction schema gained per-nozzle `mount` (shell/head-left/head-right) + `radialOffset` (mm from CL); `engine/head-nozzle-placement.ts` maps head mounts onto the ellipsoid (pos = ∓headDepth·√(1−(2r/id)²) from the respective tangent line, axial orientation via `orientationMode:'horizontal'` + azimuth 90/270 resolved from clock angle sign — convention per plans/2026-06-19 nozzle-vertical-axis-rotation); prompt/voting/verifier extended (offset < id/2 rule; shell sentinel never gates). **Compact review** — `DrawingReviewPanel` rewritten as a dense grid (`DrawingReviewRows.tsx`): one row per nozzle/saddle, quiet-by-default tints (border-only tokens: warning=missing, danger=low/flagged, success=edited), "X of Y read cleanly" header + issues-only filter, mount select switches the position column between pos and offset-from-CL, mount/offset now gate apply. Field test 2 (uniform 90° feature rotation) → Phase F: angles are now drawing-native end-to-end (0=TDC clockwise, model transcribes labels only); the single conversion `drawingClockToVesselAngle` lives in head-nozzle-placement.ts at apply time (flattened-view TDC lesson applied — no model-side or UI-side conversion, ever); review gains a Rotate-all ±90/180/Mirror escape hatch. Field test 3 (zero nozzles) → prompt restored toward proven-good wording (geometric angle estimation permitted, names pinned to TAGs) + voting never silently discards singleton nozzles (1-of-3 → low + 'single-pass' flag). Field test 4 → Phase G horizontal nozzles: `nozzleOrientation` required classification + `elevation` (EL from CL); placement derives shell angle asin(2EL/id) per facing side and corrects projection for the center-plane convention (proj_engine = proj_drawing + R − √(R²−EL²); engine consumes proj as axis-referenced stick-out). Also fixed post-launch: the vestigial eager `reviewToLenientResult` call threw on missing vessel ID before the review screen could open — extraction now always delivers the review and strictness lives only at apply. Original phases: GA→3D import hardened after research audit (crop→VLM shape validated against production drawing-digitization practice; the gap was the single unverified pass). Extraction now runs a 3-call Gemini ensemble (temp 0.7, seeds 41-43, enforced `responseSchema`, JSON.parse only) reconciled per-field by pure voting (`engine/drawing-extraction-voting.ts`, agreement→high/medium/low/missing confidence) + rules verifier (`engine/drawing-verifier.ts`: plausibility ranges, standard-NPS flag-never-snap, duplicate/unmatched tags). **Silent defaulting deleted** — unreadable fields are `missing` and block apply until user-edited in the new editable `DrawingReviewPanel` (confidence badges, per-field edit, nozzle/saddle removal, PDF page selector). Apply path: `pipelines[].nozzleIndex` remapped by nozzle name via `engine/nozzle-ref-remap.ts` (unmatched → user-confirmed removal, never silent), proj/size/saddle-pos clamps added to `validateVesselState`. Dead `RegionSelector.tsx`/`DrawingResultView.tsx` deleted. 45 new unit tests + 1 RTL test. Model stays Gemini Flash tier via `MODEL_CANDIDATES` fallback list (verify newer Flash IDs against ListModels before prepending). Deferred: cross-model second opinion, CV leader-line pre-pass, provenance persistence.
- [[plans/2026-07-30-vessel-modeler-ux-roadmap]] - approved vessel-modeler backlog (2026-07-30): T1 appendage completion (finish Phase 3 + unified dome/body-aware sampler + Phase 4 attachable parity) → T2 foundations (vessel-coords.ts, VesselModeler decomposition, structural-hash fix, stable nozzle ids) → T3 UX batch (undo labels, view cube/bookmarks, outliner, command palette, clip planes); TopologyViewer absorb recommended (pending), CML layer (B6) parked. Verified 2026-07-30: dome-aware annotation stats/heatmap NOT implemented (only geometry drapes are — sampling still pure-cylinder; annotation-heatmap still holds the comment-synced duplicate sampler); appendage Phase 3 stalled after T1 (junction-footprint.ts had zero callers). T0 branch-hygiene commits landed 2026-07-30 (five interleaved workstreams committed in attributed chunks).
- [[plans/2026-07-29-vessel-modeler-undo-redo-design]] - IMPLEMENTED same day (all 3 phases, uncommitted on `feature/appendage-bodies`): snapshot undo/redo over the immutable `vesselReducer`, vessel slice only. New `engine/vessel-history.ts` (pure past/future stacks, cap 50, coalesce same-key <1s; 12 unit tests) + `useTextureRehydration.ts` (rebuilds disposed THREE textures from `imageData` on undo — must store into a NEW `textureObjectsRef` object, ThreeViewport detects texture changes by identity). Domain wrappers auto-derive coalesce keys (`entity:id:sortedFields`) so drag storms AND per-keystroke sidebar edits collapse to one entry; `onDragEnd` dispatches `HISTORY_BREAK`; the 4 cloud-rehydration dispatches are `skip`-tagged; `SET_VESSEL` clears history (load = boundary). UNDO/REDO reset selection/draw/previews and re-derive `ui.labelsTidied`; `modelMode` syncs from `vesselShape` via effect (mode toggle guarded against redundant-click spurious entries). Ctrl+Z/Ctrl+Shift+Z/Ctrl+Y with text-input guard + toolbar buttons. Verified: build/typecheck/lint clean, 12/12 + 61 serialization/structural-hash tests, in-place-mutation audit CLEAN, 9-scenario headless Playwright drive all PASS. Known pre-existing failures NOT from this work: `personnel-service.test.ts` assertion (parallel-session personnel changes), `useLayoutMode.test.ts` OOM flake.
- [[plans/2026-07-28-competency-attribution-and-session-hardening-design]] - incident 2026-07-28: an admin filed Ben Wilkes' TOFD L2 cert under Richard Biggar's profile (human error — RLS worked as designed; full 3-agent forensic sweep confirmed no policy, session, or edge-function defect). Response feature: `employee_competencies.created_by` (tamper-proof BEFORE INSERT trigger, FK → profiles for name embeds; migration `20260728130000`), `audit_row_change()` now records `details.on_behalf_of` when the actor writes a row owned by someone else (INSERT details were previously NULL — actor was logged but not target), ActivityLogTab "for {name}" + on-behalf filter, "Added by {name}" on competency cards, storage INSERT policy gains `super_admin` (`20260728131000`, was admin-only — under-permission). Session hardening from the same investigation: header now shows logged-in name+role (LayoutNew previously rendered NO identity), restored-session "Not you?" banner, auth-supabase stale-identity guard fixed (`session.user.id !== currentUser?.id`, was `!this.currentUser`), `assertActiveUser` guard on ProfilePage self-service writes, AUTH-DEBUG PII logs stripped. No-op auth lock rework deliberately deferred (see Decision Log). Historic forensics: correlate `activity_log` actor (action_type `competency_created`) with the row's `created_at`. **Both migrations applied to live Supabase 2026-07-29** (`db push`, verified via `migration list`) — server-side attribution capture is live for all inserts including from the old deployed frontend; the UI surfacing ships with the next frontend deploy (code uncommitted as of push).
- Admin create-user 400 fix (2026-07-28): edge-function error bodies were unreadable client-side — supabase-js v2 `FunctionsHttpError.context` is a raw `Response`, so `context?.error` is always undefined; `auth-users.ts` now parses `context.json().error` (`extractInvokeError` + regression test). `CreateUserModal` now validates via shared `validatePasswordStrength` (gate on `feedback.length` — `isValid` does NOT cover the user-info/common-password checks), mirroring the server-only rejections that caused opaque 400s. `create-user` edge fn: `manager` added to VALID_ROLES, `super_admin` creation gated to super_admin actors, invalid roles are explicit 400s instead of silent viewer downgrade — edge fn deployed to prod same day (CLI 2.107.0, smoke-tested live).
- [[plans/2026-07-28-competency-multi-documents-design]] - certifications now support multiple documents/pages: child table `competency_documents` (+ RLS mirroring parent, backfill) with parent scalars mirrored to first doc; multi-file upload field, paged viewers in profile card + both review modals; migration `20260728120000` should be applied to live Supabase before/with the frontend deploy; reads are self-healing regardless (PGRST200 → `withCompetencyDocumentsFallback` retries once without the embed, legacy single-doc mode until reload — added same day after the personnel page hard-failed pre-migration)
- [[plans/2026-07-24-annotation-dome-mapping-design]] - annotations now map onto dome ends in meridian arc-length space (`engine/dome-arc.ts`), true mm footprint, seamless shell-dome crossing; addendum same day: apex reachability (radiusAt clamp 0.99 -> 1.0, the 0.141R untouchable disk at dome centres is gone); stats/heatmap dome-awareness is a recorded follow-up
- [[plans/2026-07-21-cscan-mm-less-filename-arbitration-gap]] - BRT V-1001 root cause + arbitration v2 (bare tokens, Data File source, metadata halving, localZero anchor, overlap cross-check, truncation warnings)
- [[plans/2026-07-12-tools-projects-comprehensive-review]] - verified review + pivot analysis of Tools/Projects (18 claims adversarially verified; roadmap Phases 0-4)
- [[plans/2026-06-11-cscan-offset-filename-arbitration]]
- [[plans/2026-05-01-industrial-theme-handover]]
- [[plans/2026-04-30-industrial-instrument-handover]]
- [[plans/2026-04-23-vessel-overview-page-implementation]]
- [[plans/2026-04-23-scan-viewer-landing-implementation]]
- [[plans/2026-04-16-paut-report-pdf-build-plan]]

## Working Rhythm

- Capture rough ideas in [[inbox/README|Inbox]].
- Promote implementation plans to `docs/plans/YYYY-MM-DD-topic.md`.
- Capture lasting architectural choices with [[templates/Decision Record|Decision Record]].
- End substantial work with [[templates/Handover Note|Handover Note]] so the next session has a clean trail.
- For agent continuity, update [[Agent Memory]] only when a change affects future project orientation.

## Useful Code Landmarks

- App routing: `src/App.tsx`
- Vessel modeler: `src/components/VesselModeler/`
- Vessel model services: `src/services/vessel-model-service.ts`
- Project workflow: `src/pages/projects/`, `src/components/projects/`
- Scan viewer: `src/components/projects/scan-viewer/`
- C-scan visualizer: `src/components/CscanVisualizer/`
- Supabase client: `src/supabase-client.ts`
- Database scripts: `database/`, `supabase/migrations/`
