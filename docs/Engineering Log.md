---
tags:
  - engineering/log
  - ndt-suite
aliases:
  - Engineering Log
---

# Engineering Log

Use this page as the running handoff point between Obsidian notes and code work.

## Active Areas

- Vessel modeler coverage, piping, viewport rendering, and material handling.
- Industrial theme polish across admin, documents, personnel, projects, profile, demos, and layout styles.
- Clean design rollout: login page restyled 2026-07-16 (`src/pages/login.css`, `lg-` prefix) to match personnel/projects/profile.
- Email deliverability to Outlook/M365: code hardening extended 2026-07-16 to client senders + auth templates; committed (`ed0ab75`) and the 4 sender edge functions deployed. Auth templates still need re-pasting into the Supabase dashboard; corporate allow-list/quarantine check outstanding.
- Project inspection workflow, report readiness, scan viewer, and companion app integration.
- Supabase migrations, storage policies, and user/account security workflows.
- **Agent memory engine (`iai-pme` 3.0.4) installed 2026-08-20 - machine-local, NOT repository code.** Venv at `C:\Users\jonas\.iai-pme` (prebuilt `cp311-win_amd64` wheel, Python 3.11; the upstream `bootstrap.sh` hard-refuses Windows and Windows support is upstream-marked BETA, so the manual path was used). Store at `~/.iai-mcp` (AES-256-GCM per record, `.crypto.key` mode 0600), daemon bound on loopback with a token handshake. MCP server `iai-mcp` registered at USER scope in `~/.claude.json`. Four ambient hooks (Stop session-capture, UserPromptSubmit turn-capture, UserPromptSubmit per-turn-recall, SessionStart recall) patched into the GLOBAL `~/.claude/settings.json`, so they apply to EVERY project on this machine, not just this repo (pre-install backup at `~/.claude/settings.json.bak-pre-iai`; this repo's own orchestration `UserPromptSubmit` hook lives in `.claude/settings.json` and is untouched). Seeded with the repo memory layer: `docs/agent-memory/*`, `docs/Engineering Log.md`, `docs/Agent Memory.md`, `CLAUDE.md`, `.claude/CLAUDE.md`, `AGENTS.md` plus all 74 `docs/plans/*.md` = 81 files / 2067 chunks / 25.5 MB `brain.sqlite3`, zero failures. Gate: `iai-mcp doctor` 32/32 PASS, `claude mcp list` connected, semantic recall verified through the daemon (cue "vessel modeler coverage comparison scope planning" returns the scope-planning handoff at 0.980).
- **Three Windows defects in `iai-pme` 3.0.4 had to be fixed locally to get that working - documented at `~/.iai-mcp/local-patches/README.md` and SELF-HEALING: `ensure_patches.py` detects each bug by its broken code pattern (no-op once upstream fixes it) and runs from `daemon-start.cmd` before every boot, so a package update that overwrites site-packages is re-healed at the next daemon start; `backup/` there holds the crypto key, task XML, and hooked settings.json. Caveat: `daemon install` regenerates daemon-start.cmd, dropping the guard line - see the README.** (1) `daemon/__init__.py` `_install_boot_signal_trace` iterates `(signal.SIGTERM, signal.SIGINT, signal.SIGHUP)` - the tuple is built before the loop body, so the per-signal `except AttributeError` guard never fires and the Windows-absent `SIGHUP` killed the daemon at boot. (2) The real one: `socket_server.py` `SocketServer.serve` probed `inspect.signature(asyncio.start_unix_server)` ABOVE its `if IS_WINDOWS:` branch, and asyncio exposes `start_unix_server` only where AF_UNIX exists - so every Windows boot raised AttributeError before the loopback bind. `serve()` is scheduled fire-and-forget, so it failed SILENTLY: the daemon booted, warmed, ticked its FSM and served nothing; `.daemon.port` was never written, every client reported "daemon not running", `iai recall` degraded to a substring bank-scan (0.000-score, off-target hits) rather than erroring, and the watchdog killed the process `reason=wedge` at the 600s cold-start grace on every boot. Fix is to move the POSIX capability probe below the Windows `return`. (3) `iai-mcp daemon install` generates a Task Scheduler XML whose `<LogonTrigger>` and `<Principal>` carry no `<UserId>`, which Windows reads as "at logon of ANY user" and therefore requires elevation - `schtasks /Create` returns "Access is denied" for a normal user. The task `iai-mcp-daemon` here was registered by hand from a patched XML pinning both to this user; `daemon start/stop/status/uninstall` address the task by name and work against it normally. A FOURTH defect found 2026-08-21: the four ambient hooks are POSIX shell scripts needing jq or /usr/bin/python3 plus fcntl-python - none exist on Windows, so ambient capture/recall NEVER ran (hooks died silently; recall's bank-scan fallback masked it). Fixed with `~/.claude/hooks/iai-win-hook.py`, a venv-python shim dispatching in-process to the Windows-ported CLI backends (capture-turn-deferred / session-refresh-if-stale / session-start / capture-transcript --no-spawn); all four settings.json hook commands now call it directly, breadcrumbs in `~/.iai-mcp/logs/hook-shim-*.log`. Verified: this session's turns visible in `iai last`. All four are worth reporting upstream - the README explicitly asks for Windows issues and PRs.

## Recent Handovers

- [[plans/2026-08-28-beamtool-integration-design]] - **NotBeamTool ported into the suite 2026-08-28 (committed to master same day — the BeamTool files went to master as a surgical commit, the held 2FA-gate/security frontend stayed uncommitted):** the standalone UT angle-beam technique designer (`C:\Users\jonas\OneDrive\Documents\Notbeamtool`, React 18 + plain SVG, zero runtime deps) now lives at `src/components/BeamTool/` with route `/beam-tool` (tools tab, lazy, guard shape identical to `/topology`) + Tools nav entry "NotBeamTool". Verbatim port keeping the tool's current styling — containment only: every CSS rule scoped under `.nbt-page`, and ALL 33 tool classes `nbt-`-prefixed after the suite's global `.sidebar { position: fixed; left: 0 }` (layout.css:253) captured the tool's unprefixed sidebar and pinned it to the LEFT (user-visible bug, fixed same day; `.app`/`.header`/`.btn` were the same class of collision). Tool keeps its own dark/light toggle but stamps `data-nbt-theme` on the page root — NEVER `documentElement`, whose `data-theme` belongs to ThemeContext. PNG technique-sheet export reads computed style from the SVG element (custom props no longer on `:root`). One type adaptation: @types/react 19 `RefObject<T | null>`. Prefix-boundary rule: prefix where a string becomes a className (`Readouts.tsx` builds `nbt-` + `cls`), never in data (`sheet.ts` `cls: 'plain'|'warn'` also drives canvas fillStyle; `accent === 'amber'` is a prop comparison). Deferred styling pass recorded in the design doc (suite token/font mapping, element-selector bleed like `input[type=…]`, max-lines splits — Canvas 805 / Sidebar 357 warn-only). Gate: typecheck ✓, lint 0 errors, build ✓ (own ~30 kB chunk), suite 2143 passed | 3 skipped (sole failure = documented useLayoutMode OOM), CI-mode coverage 145/145 green 65.09%, verify:share-chunk OK, smoke test re-run in main loop 2/2.
- [[plans/2026-08-26-security-hardening-and-platform-ops-plan]] - **RESIDUALS ARC (2026-08-27, third package): org_admin approval defect FIXED+DEPLOYED, avatars M11 refactor landed, polish batch closed.** Owner pinned backups (platform dailies suffice for now; scripts ready). **DEPLOYED:** migration `20260827120000_profiles_role_guard_definer_context.sql` (adversarially reviewed SHIP; owner-equality + dependency assertions both passed on prod = vetting provably engaged) — defect CONFIRMED by container repro (auth.uid() reads the JWT GUC, unchanged under SECURITY DEFINER, so the guard judged the org_admin caller inside the vetted RPC); fix = guard converted to SECURITY INVOKER + owner-comparison vetting (session-GUC rejected as client-settable; **service_role deliberately NOT vetted — authenticator is a member, vetting it would hand the exemption to any service-key holder**); en route closed a pre-existing NULL-role three-valued-logic hole (JWT with no profiles row could mint super_admin through definer fns). **Escalation CLASS closed:** weaker hand-run approve_permission_request copies neutralized in supabase-profile-schema.sql + security-audit-fix-2026-02.sql (post-migration they'd have been full escalation chains — review MAJOR-1); new Decision Log invariant: definer fns updating role/organization_id carry their OWN authorization, trigger won't second-guess. Accepted pending owner review: org_admin can approve in-org org_admin requests (20260812120000's deliberate allowlist, unmasked by the fix). Ledger blind spot recorded: policy_defs_md5 covers pg_policies only — trigger changes invisible, evidence via pg_proc. **M11 avatars (in tree):** `avatar-service.ts` single reader + `useAvatarUrls` batched signing; uploads store PATHS; both-shapes tolerant contract (legacy public URLs passed through UNTOUCHED — never re-signed — so deploy order can't couple to the backfill); exactly TWO render sites (ProfileAvatar/PersonAvatar; LayoutNew renders no avatar — brief's hypothesis corrected); GDPR erasure fns verified both-shapes-tolerant already; bucket flip + conservative regex backfill PARKED in `database/parked-migrations/avatars_private.sql` (GATE 1 classification incl. pdf_valued count for the audit's legacy-PDF vector; apply only WITH the frontend ship). **Polish:** object-src 'none' in vercel.json AND vite dev/preview (evidence: zero object/embed anywhere, all PDF previews are iframes/pdf.js canvas); contact emails single-sourced in `src/config/contact.ts`; dead Require2FA deleted; share page now writes ZERO storage keys (admin config lazy via ensureLoaded); **Modal white-on-light ROOT-CAUSED as dev-only duplicate CSS injection** — LoadingStates.tsx's redundant glassmorphic import injected a second copy after main.css in dev, re-declaring --text-primary white for all 20 modals; prod byte-identical before/after (new Decision Log entry: global CSS imported once, in main.css only; dev/prod token divergence = check duplicate style nodes first). REFUTED en route: avatars agent's "useLayoutMode.test.ts is gone / flake moved" — file exists, still OOMs alone, documented-flake stance unchanged. DEFERRED to post-ship arc (recorded reasoning): npm major bumps, separate share HTML entry. Backlog adds: GDPR export resolves avatar path→signed URL, GIF mime mismatch, stale 2MB error string. **FINAL INTEGRATED GATE (main loop, combined tree — required because avatars+polish worked src concurrently): build ✓ 1m58s, lint 0 errors, suite 141/142 files 2106 passed | 3 skipped (sole error = documented useLayoutMode OOM), verify:share-chunk ✓.** **LATE ADDITION same day — backups UNPINNED, destination = partitioned S3 in the owner's AWS account (senior-engineer directive):** `db-backup.ps1` gained an off-site Step 7 (date-partitioned keys `ndt-backups/db/year=/month=/day=/`, manifest sidecar carrying the sealed archive's own sha256, storage bytes streamed Supabase→AWS directly via rclone copy-only), `db-restore.ps1` gained `-FromS3` (separate offline reader keys, re-hash before the passphrase is spent); exit-code contract distinguishes local-only (4) from upload-failed-nothing-pruned (5); **write-only IAM posture** (writer has no Get/Delete — the three rclone flags `no_check_bucket`/`no_head`/`--no-check-dest` are REQUIRED by that policy, not preferences; `RCLONE_S3_*` backend env vars are BANNED — they outrank rclone.conf on every s3 remote and would leak Supabase creds onto the AWS destination; remote-scoped `RCLONE_CONFIG_NDTSUPA_*` only); one-time console runbook `docs/processes/aws-backup-setup.md` (least-privilege policy JSONs, lifecycle Deep-Archive-30d/expire-90d with the 180-day-minimum billing caveat, Object Lock decision at creation). Main-loop security review done in lieu of the refused automated pass (no injection, no stray creds, policies verified least-privilege); both scripts parse 0 errors. Blocking first green run: rclone install + AWS console setup (owner). + P1.3/P1.4 STAGED (2026-08-26/27, second arc; frontend ship still held — owner rule: dev-branch Vercel preview before master).** **DEPLOYED to ntrg:** migration `20260826150000_db_state_ledger.sql` (adversarially reviewed, SHIP + MAJOR-1 folded pre-push: `db_state_snapshots` write access REVOKEd from every API role **incl. service_role** — BYPASSRLS means the no-write-policy defence doesn't bind it, so the grant is the only barrier on the evidence table) — nightly `capture_db_state()` 02:17 UTC (per-table counts, deterministic pg_policies md5 = continuous M12 evidence, per-bucket storage counts, migration tail, 400d self-prune) + **`activity-log-retention-nightly` 03:43 UTC finally scheduling `scheduled_purge_activity_logs(730)` (M8 closed pending first-run proof; legacy never-activated job name unscheduled)**. First-run evidence to check the morning after: one snapshot row + one 0-row-purge self-audit row; silent-cron history means schedule ≠ firing. **Resurrection-vector class CLOSED (review MAJOR-2):** `cleanup_old_activity_logs()` neutralized with DO-NOT-RE-ADD blocks in ALL THREE hand-run scripts (`data-retention.sql` — which also lost its 3y-vs-730d drift — plus `activity-log-schema.sql` + `migrations/security-fix-definer-functions-2026-02.sql`); data-retention's three definer fns gained search_path pins + PUBLIC/anon/authenticated EXECUTE revokes (MINOR-3). **P1.3 PARKED-FINAL:** `database/parked-migrations/aal2_enforcement.sql` (outside supabase/migrations so db push can't apply it — parking verified against every mechanism) — 31 tables, 37 RESTRICTIVE policies, container-probed on live Postgres 17. TWO design catches en route: (1) the spec'd uniform policy would have **bricked login for everyone** (loadUserProfile reads profiles/organizations at aal1 BEFORE the TOTP challenge — auth-supabase.ts:158) → bootstrap pair widened to own-row-at-aal1; (2) FOR ALL reuses USING as WITH CHECK → own-row *writes* at aal1 → final shape = per-command split (SELECT widened; INSERT/UPDATE/DELETE hard) because a hard FOR ALL measurably re-broke reads (restrictive policies AND together — recorded in-file, never "simplify" back). Applies ONLY after the readiness query returns zero unenrolled active users. **P1.4 client landed** (`session-timebox.ts` pure model, expiry banner keyed to session start, forced-expiry via SIGNED_OUT authority, dead SESSION_CONFIG deleted — zero consumers verified); server timebox flip = production-ship step. **P3.1/3.2:** `scripts/db-backup.ps1`/`db-restore.ps1` (storage-policies captured at BACKUP time as 6th artifact — a real disaster has no live catalog to regenerate from; Invoke-Native stderr-demotion fix; gates proven non-vacuous via tamper tests) + `docs/processes/backup-and-restore.md`/`disaster-recovery.md`; restore test P3.4 still owed (needs credentials); **rclone not installed → storage bytes (~1.37GB) have no off-platform copy yet**. **P5.1:** seven runbooks in docs/processes/ (found: tsconfig path aliases are dead config — no vite resolve.alias, zero users; Vercel prod-branch setting inferred, confirm in dashboard before the dev-branch preview deploy). **P5.2:** breach register CREATED with BR-2026-001 (2026-07-28 incident; recommended no-notify, owner sign-off pending), third-party-dpa.md rewritten (6 processors; false "no other processors" claim gone), DPIA 6-role/eu-west-2 refresh, infosec policy v1.0 draft, risk-register.md seeded (70 REVIEW-owner markers across the set; controller-name styling drift found: "…Inspection Services" vs bundle-types "…Inspections"). **P4:** [[plans/2026-08-26-multi-app-platform-design]] drafted (cookie sessions on .matrixportal.io = SSO + closes L1; blocked on owner inventory §2). Suite evidence current at 2080 passed | 3 skipped (last src change; SQL/docs since). OWNER QUEUE: teardown ~08-31, dashboard backup confirm, cron first-run check, dev-branch preview ship → timebox flip + GoTrue rate-limit verify → enrollment confirm → un-park aal2 (fresh timestamp + re-review).
- [[plans/2026-08-26-security-hardening-and-platform-ops-plan]] - **PHASES 1+2 EXECUTED same day (2026-08-26): 2FA backend LIVE on ntrg, mandatory-enrollment frontend complete in tree, share-exposure review CLOSED.** Owner decisions: immediate ALL-roles 2FA, daily backups (no PITR), autonomous execution. **Premise correction, live-defect class:** `admin-reset-2fa` + `manage-backup-codes` had NEVER existed anywhere (not ntrg, not the old project, not git history) — the admin Reset-2FA button 404'd in prod its entire life and authenticator loss was a permanent lockout. Both authored fresh (role-rank + no-self-reset gating; hashed single-use Crockford-32 codes; batch-salt PBKDF2 — one salt per batch or verification blows the edge CPU budget; aal2-gated minting via fail-closed `_shared/jwt-claims.ts`), adversarially reviewed (migration SHIP; fn DO-NOT-SHIP until insert-first batch ordering closed a real regenerate lockout window; the aal-decode justification attacked four ways and held), then DEPLOYED: migration `20260826120000_two_factor_backup_codes` pushed, both fns live + 401-probed. **Redemption = self-service 2FA reset (owner-ratified, never re-litigate):** factors deleted FIRST, whole code set consumed second — failure consumes nothing; concurrent second redeemer tolerated (`factor-errors.ts`). Rationale: the app derives 2FA-cleared solely from the JWT `aal` claim, which an edge fn cannot mint, so "mark used" was a proven dead end; reset composes with the gate (redeem → re-enroll → aal2) and future P1.3 aal2 RLS. Frontend in tree (ships with next deploy): `RequireTwoFactorEnrolled` wrapping all protected routes — gate-shell-first after the browser drive proved the auto-open wizard overlay blocked the Sign-out hatch (RTL was green; drive caught it) — wizard backup-codes step, `TwoFactorChallenge` login recovery path, unverified-factor cleanup (was DEAD CODE: auth-js 2.78 buckets only verified factors into `data.totp`, everything lives in `data.all`; invented mock shapes hid it — selection rules single-sourced in `src/services/mfa-factor-shape.ts`, mocks now mirror the library loop), FunctionsHttpError scar fixed in two-factor-service + `adminReset2FA`, ReactQueryDevtools DEV-gated. **Runtime-verified headless Edge 6/6** incl. F-20: captured network log shows ZERO /auth/v1/ requests on a /share visit. Share exposure report [[plans/2026-08-26-share-exposure-report]]: verdict clean (one JWT in the whole build = anon key, decoded not grepped); **F-16 REFUTED — localhost:18923-18932 in prod CSP are the Companion app's production ports, never remove**; `X-Robots-Tag: noindex` added for /share/* (re-probe post-deploy). Final gate (main-loop confirmatory run on the integrated tree): build ✓ tsc clean, lint 0 errors/408 baseline warnings, suite 137/138 files 2048 passed | 3 skipped (sole error = documented useLayoutMode OOM flake), verify:share-chunk OK. **NEXT:** frontend deploy = commit + merge → Vercel (**owner call — the mandatory gate hits every user the moment it ships**); P1.3 aal2 migration ONLY after live factor counts confirm full enrollment; P1.4 12h timebox + modeler expiry-warning UX; P0 teardown (~08-31) still owner-blocked. Backlog: shared Modal title contrast (live diagnosis, don't blind-edit shared UI), SECURITY DEFINER atomic batch-replace RPC, role smoke ×6, TOTP-replay + refresh-chain live smoke.
- [[plans/2026-08-25-client-share-stats-panel-design]] - **IMPLEMENTED 2026-08-25/26 on `feature/scope-planning-suite` (uncommitted):** the client share viewer now carries the modeler stats panel's FULL content (owner ask "our version of the stats panel in the client viewer"). Manifest vessels ship finished numbers per rule 3 (full-state, publish never changes a number): `ShareStatRow` gains `rbaPct` (set only when a target entry exists — never 0-coerced), `totalMm2`/`targetMm2`/`achievedMm2`, `targetAuto` (rect-derived marker); new optional `wallLoss` (self-contained wire types in bundle-types, never aliased to worker types) built by `buildShareWallLoss`, which runs the pure `compute()` SYNCHRONOUSLY at publish via NEW `src/workers/wall-loss-request.ts` (`buildWallLossRequest`/`canComputeWallLoss` extracted verbatim from `useWallLossWorker`, which now consumes it — ONE request source, panel/bundle drift impossible by construction). Wall loss is an aggregate deliverable like achievedPct: computed from the full state, ships even when the scans layer is unticked. **NO formatVersion bump** — all additions optional; old bundles degrade to the old table with no wall-loss section, old viewers ignore the new fields. Viewer side: `ShareStatsTable` upgraded (Feature · RBA · Target · Achieved · Δ · Status, stacked %-over-m² cells, `auto` marker, `<tfoot>` total only when every row shipped areas, status word-beside-dot kept) + new `ShareWallLossSection` (content mirror of `WallLossStatsSection`: swatch ramp, spurious/total rows, per-body selector; imports ONLY react + bundle-types + the proven-safe formatters — helpers copied on purpose), both under one `cs-statistics` block; css `cs-`-scoped per the scar. **Preload-helper chunk scar found+fixed en route: `verify:share-chunk` had been silently RED since 2026-08-13** — annotation-labels.ts's deliberately-lazy service `import()` made Rollup park `vite/preload-helper` INSIDE the object-form `supabase-vendor` manual chunk, giving pipeline-geometry (statically in the share closure) an ESM edge onto supabase-js (~42kB gz) on the loginless page. `vite.config.js` `manualChunks` is now FUNCTION-form pinning the helper to its own ~1.1kB chunk (tslib explicitly kept in supabase-vendor; react jsx-runtime/react-dom-client moved entry→react-vendor; bonus: the ClientSharePage closure no longer touches the entry chunk at all — the guard's documented entry-exclusion compromise is moot for the static graph). Never return manualChunks to object form without re-running the guard. Latent flagged, NOT fixed (behavior-preserving refactor): `useWallLossWorker`'s effect deps omit `vesselState.orientation`, which `mainShellNozzleFootprintParams` reads — pre-existing live-update gap, separate decision. Gate: typecheck ✓, build ✓, `verify:share-chunk` OK, CI-mode suite 131 files / 1952 passed | 3 skipped (the excluded `useLayoutMode` OOM is the documented flake), eslint 0 errors on touched files.
- [[plans/2026-08-26-security-hardening-and-platform-ops-plan]] - **PROPOSED 2026-08-26 (docs only, nothing implemented):** merged senior-engineer findings (mandatory 2FA, 12h session cap, share-page HTML exposure review, scripted backup+restore, interval DB state logging, subdomain sibling apps, process READMEs) with the same-day ISO 27001-style posture assessment (tech controls strong; ISMS layer absent — no policy/risk register/SoA, breach register has zero entries incl. the 2026-07-28 incident, supplier doc falsely claims "no other processors", no backup evidence on ntrg). Phased: P0 = teardown + platform backups before ~08-31 (hard deadline); P1 = recover the two missing 2FA edge fns → mandatory enrollment → restrictive `aal2` RLS (order is load-bearing: enforcement before full enrollment = org-wide lockout) → 12h timebox (+ GoTrue rate-limit verify, L3) with modeler expiry-warning UX; P2 = share-page exposure report (+ X-Robots-Tag noindex, separate-HTML-entry decision); P3 = db-backup/db-restore scripts off the proven runbook sequence + nightly pg_cron `db_state_snapshots` (pure SQL, no HTTP — dodges the 401 scar; pg_policies md5 = continuous M12 evidence) + schedules the M8 retention purge; P4 = subdomain platform design doc (cookie sessions w/ Domain=.matrixportal.io = SSO enabler AND closes L1 — one workstream; HSTS preload already binds subdomains to HTTPS); P5 = docs/processes/ READMEs + breach register/supplier/placeholder fixes + residual backlog. Owner decisions flagged inline (PITR, rollout shape, inactivity timeout, retention window, cert-vs-readiness, org_admin approval fix). commits `d0d4fac` + `61d8067`, pipeline green after): the branch's first GitLab pipeline failed ONLY `src/utils/__tests__/client-share-passcode.test.ts` — green on every Windows machine (Node 22) because the failure needs the CI runner's Node 20. Two layers: (1) `src/test/setup.js` installed a hand-rolled `crypto` mock whenever `global.crypto` was absent (subtle had only a stub digest — no importKey/deriveBits); absent is exactly the CI case, so PBKDF2 threw instantly. The fallback is now Node's REAL `webcrypto` (installed when `crypto.subtle` is missing) — never re-add a partial crypto mock. (2) With real webcrypto in place, Node 20 under jsdom still rejected the browser passcode mirror's salt because it was passed as a detached `.buffer.slice()` ArrayBuffer copy (realm-sensitive instanceof in Node 20's BufferSource check; Node 22 tolerates it). The Deno mirror passes the `Uint8Array` VIEW directly and its identical test was green on the same runner — the browser mirror now matches (salt typed `Uint8Array<ArrayBuffer>`, no slice). Hash format unchanged; the cross-runtime contract test pins both sides. Lesson: local Windows/Node-22 green does not cover the Node-20 runner — anything WebCrypto-shaped should hand views, not sliced buffers, to `crypto.subtle`.
- Stale-cache "rects don't save" fix (2026-08-21, `feature/scope-planning-suite`): the modeler's Update button saved coverage rects correctly (serializer round-trip verified, 91 tests), but `useUpdateVesselModel`/`useSaveVesselModel`/`useDeleteVesselModel` invalidated only the `['vesselModels']` key family — the scope-planning surfaces read the model through `['linkedVesselModel', …]` (`useLinkedVesselModel`, `useVesselPlanningSummary`) and `['vesselModelForReport', …]`, both with 5-min staleTime, so after Update the planning strip / coverage-scope panel / report page kept serving the pre-save config on SPA navigation and looked like the save was lost. All vessel-model mutations now invalidate those families too (`useVesselModelMutations.ts`); share publish was never affected (fetches the record directly, not via query cache). Gate: typecheck ✓, lint 0, suite 1899 passed | 3 skipped (only the documented pre-existing `useLayoutMode.test.ts` OOM worker crash).
- [[plans/2026-08-21-rect-derived-scope-design]] - **IMPLEMENTED same day on `feature/scope-planning-suite` (uncommitted).** Owner ruling: drawn coverage rects ARE how coverage is scoped — Scoped % is now rect-derived (`covered/total` from the real rasters) whenever a feature has rect coverage, with the manual `scopedPct` entry as fallback and boot domes permanently manual (no dome-rect raster exists). SUPERSEDES the 2026-08-20 "scopedPct is THE target" amendment; `targetPctOf(entry, rectCoveredMm2, totalMm2) → ResolvedTarget|undefined` in `coverage-comparison.ts` stays the single decision point, rows carry `targetSource`/`targetMm2`, new `computeRegionCoveredAreas` projection in coverage-calculator. Modeler UI: the THREE overlapping stats sections (Coverage / Scan Coverage / Coverage vs Scope) merged into ONE `stats/CoverageScopeSection.tsx` (RBA · Scoped · Achieved · Δ/status per feature incl. boot-dome rows, untracked = "—" never 0, `auto` marker on rect-derived); toggle flags collapsed to `showStatsCoverage`/`TOGGLE_STATS_COVERAGE`; `CoverageTargetsEditor` disables Scoped (read-only derived + `auto`) when rects cover a feature. Downstream surfaces (projects strip/tab, docx report, share stats) pick the change up through the engine unchanged. This resolves the modeler half of the "two coverage vocabularies" open item; the projects-side legacy `utils/coverage-calc.ts` number (`ScopeProgressCard`/`ScopeSection`) is still the named follow-up. Gate: build ✓ (1m24s), suite 125 files / 1846 passed | 3 skipped (CI exclusion), lint 0 errors.
- [[plans/2026-08-17-client-sharing-design]] - **Phases 4a + 4b implemented 2026-08-20 on `feature/scope-planning-suite`. BACKEND DEPLOYED later the same day** to eu-west-2 cutover target `ntrgjqrbewbvwofupphn` (see the scope-planning handoff's deploy record; **CORRECTION 2026-08-24:** the caveat originally recorded here — "production frontend still points at the old eu-north-1 project, prod flip gated on the final data sync" — was stale context: the desktop's cutover runbook was uncommitted, and prod had already been flipped to `ntrgjqrbewbvwofupphn` during the 2026-08-17 cutover with the old project frozen read-only since. Verified 2026-08-24 against the live matrixportal.io bundle, which references only the new ref. No final data sync is pending). Original record follows: *(pre-deploy)* — the migration and edge function are repository code only, gated on the Supabase region cutover; the ordered deploy checklist is the top comment of `supabase/migrations/20260820120000_client_shares.sql`. No `db push`, no `functions deploy`, no `link` was run. **4a (backend):** `client_shares` + `client_share_views` + the private `client-shares` bucket. `organization_id` AND `bundle_path` are trigger-DERIVED, so a caller's values are discarded rather than validated (Postgres evaluates WITH CHECK on the row as modified by BEFORE triggers, so a cross-org insert is caught by the policy); every policy routes through the SECURITY DEFINER helpers, never inline `profiles` sub-selects; `anon` is explicitly REVOKEd; the view audit has no write policy for any role. `serve-client-share` proxies bundle BYTES rather than issuing signed URLs, keeping the bucket unreachable from outside the function; `bundle-path.ts` is the containment boundary (deny-by-default allowlist, 26 tests written as an attack list). **4b (frontend):** publish dialog on `ProjectDetailPage`, bundle builder, and the public `/share/:token` page. **Deliberate deviation from the spec, flagged:** the design called for pre-baked heatmap PNGs plus separate decimated grids; the viewer already builds heatmaps from composite data, so the bundle ships ONE thing — decimated grids inside the serialized model — and the viewer renders and hovers from them. Baking PNGs would have required a headless GL capture and a new "externally supplied texture" path through ReadOnlyViewport, i.e. the viewer diverging from the modeler. **Screenshots UPDATE (same day, second machine): publish-time vessel screenshots are now IMPLEMENTED** — `vessel-screenshot-state.ts` (pure; capture renders ONLY the sanitized published-layers state — PII invariant extends to pixels) + `vessel-screenshot.ts` (SceneManager offscreen 512×512, one WebGL context per publish, dynamic-import only), best-effort in the publish mutation, cards fed via `fetchShareBlobUrl`; smoke-tested in real headless Edge. **Round 2, same day: `/share/:token` itself runtime-verified WITHOUT a backend** (real `buildShareBundle` run in-browser, served back via Playwright route interception implementing the edge function contract) — two majors found + fixed + re-driven green: the bundle shipped NO thickness grids (cloud serialization skips `data`; `attachDecimatedGrids` in bundle-builder re-attaches decimated grids post-serialization, spec files untouched; dome composites were also never decimated — now min-pooled too; post-serialization byte-level tests added) and app-global CSS made the page white-on-white (injection-order-dependent theme tokens; `client-share.css` now fully self-colored, `.cs-page`-scoped — the login `.lg-page` scar recurred). Boot-hover "bug" disproven (occlusion + labelless-shell-unmounts-readout, by design). See Module Map scars + the scope-planning handoff round-2 section. **Chunk finding:** a dynamic `import()` inside the share page made Vite hang its preload helper off the supabase-vendor chunk, pulling supabase-js into a logged-out page — those imports are static now, and `npm run verify:share-chunk` guards it. Honest limit: the SPA entry chunk loads on every route and does contain auth; only a separate HTML entry would change that, which is more than the spec asks. Gate: build clean, lint 0 errors, full suite green apart from the documented pre-existing `useLayoutMode.test.ts` worker crash.
- [[plans/2026-08-17-coverage-comparison-design]] - **Phase 3 complete 2026-08-20 on `feature/scope-planning-suite`** (layers + comparison engine + ReadOnlyViewport were phases 1-2). Projects surfaces landed: a collapsed-by-default "Coverage vs Scope" section on `VesselOverviewPage` whose `lazy()` is the chunk boundary (verified in `dist`: ProjectDetailPage and VesselOverviewPage chunks import NO three/outliner chunk; the panel gets its own `CoverageScopePanel` chunk, and three.module split out of the VesselModeler chunk as a shared one), and a planning strip + "Plan scope" action on the project's vessel ROWS. **Surface correction:** the spec named `InspectionDetailPage` / `VesselCard`; the live components are `VesselOverviewPage` and `ProjectVesselList` — `VesselCard.tsx` and `ProjectVesselsTab.tsx` are dead code imported by nothing (left untouched; deleting them is a separate call). **Chunk rule discovered here:** anything on a projects page that statically imports `coverage-comparison` drags three.js into the projects chunk, because rollup groups the engine modules together — so the row strip derives through `hooks/queries/useVesselPlanningSummary.ts`, which dynamic-imports the engine inside its queryFn. New pure engine modules (all unit-tested): `coverage-feature-framing.ts` (camera-free `FeatureTargetRef` → `ViewBounds` → iso pose; the scar is that `surfacePoint(pos, 0, -radius)` is only an axis point INSIDE the cylinder — at a closure station the profile radius shrinks and the offset overshoots to the far side, so cap centres step along the frame's own axis instead), `coverage-rect-features.ts` (guidance listing routed on the canonical `rectIsPureCylinder`, never an area measure), `layer-presence.ts` (layer chips; pinned against `buildOutlinerTree` so the two can't drift). `formatCoveragePct`/`formatCoverageDelta` were collapsed into `coverage-comparison.ts` and are re-exported by `coverage-scope-report.ts` — the duplication the wayfinder flagged is gone. Hover is diffed through a ref before touching React state (the probe fires per frame). Gate: build ✓, lint 0 errors, suite 1703 passed | 3 skipped; the only failure is the documented pre-existing `useLayoutMode.test.ts` worker crash (reproduced alone, unrelated). **Runtime-verified 2026-08-20 in headless Edge** (temp `/verify-scope` route + query-cache-seeded fixture, reverted; scripts in the session scratchpad): 10/10 scenarios incl. lazy-chunk gate, strip===section===engine numbers, pixel-exact layer-toggle restore — and it caught a real closure-framing bug (head/dome rows framed inboard, head occluded), fixed same day in `coverage-feature-framing.ts` (outboard pose along the owning frame's axis, 25–65° tilt clamp, `CAP_FIT_FACTOR` 1.4; see Module Map scar entry). The "dome-scan overlay doesn't render read-only" scare was a fixture/pose artifact — it renders and toggles exactly. Still open: Phase 4 client sharing (backend as code only — blocked on the Supabase region cutover — then the sharing frontend), the React project print report still has no coverage page, and `ScopeProgressCard`/`ScopeSection` still read the OLD rect-area `utils/coverage-calc.ts` scope number rather than the target-percentage model.
- [[plans/2026-08-17-supabase-project-migration-runbook]] - **Supabase project migration eu-north-1 → eu-west-2 (2026-08-17, in flight):** logical dump/restore of live prod `cngschckqhfpwjcvsbad` to a new eu-west-2 "Matrix Portal" project (two candidates exist: `ntrgjqrbewbvwofupphn` 2026-08-17, `oxzteqqrhggdodcnngzn` 2026-08-06 — target choice pending). Full inventory done: uuid-ossp + live pg_cron only, no Vault/webhooks/Realtime/custom roles, 8 buckets (`documents` is dashboard-created — no CREATE in repo), 15 edge fns, secrets RESEND_API_KEY/GEMINI_API_KEY/CRON_SECRET/APP_URL/ALLOWED_ORIGINS. Plan = parity migration (security-remediation worktree stays parked, deploys separately after); GEMINI_API_KEY + CRON_SECRET rotate during target secret setup; dumps live OUTSIDE repo/OneDrive (PII + password hashes); cutover freeze via existing `VITE_MAINTENANCE_MODE`; JWT secret changes → all users re-login. No local psql — dump via supabase CLI 2.107.0, restore via dockerized postgres:17 psql. **DRY RUN COMPLETE 2026-08-17, all green:** full restore into the spare project with every baseline count matched, 33 storage policies regenerated from catalog (they do NOT survive the dump), 416/416 storage objects synced byte-matched, 15 fns deployed + 401 secret-gate verified, 113 host-pinned URL rows rewritten (scans/avatars/models/vessel_images/competency-doc scalars), 7/7 Playwright app checks (login, personnel, avatar-from-new-host, 6/6 projects, documents, vessel detail w/ live coverage stats, modeler canvas). REFUTED en route: "different schema generation" agent diagnosis = org-scoped RLS artifact (inspection_projects has NO super_admin override — verification users must be in the Matrix org). **CUTOVER EXECUTED same evening — matrixportal.io is LIVE on `ntrgjqrbewbvwofupphn` (eu-west-2), ~20 min read-only window, every verification gate green** (15/15 baseline metrics, 143+33 policies, 416 objects zero-delta, cron active, 401 gate, live bundle references only the new ref). Freeze = `default_transaction_read_only` on the OLD project's authenticator role (left ON permanently — protects against stale-tab writes; `VITE_MAINTENANCE_MODE` is a tools-only PII-lockdown, NOT a site-down switch). Auth config + hardened templates + SMTP-on-new-Resend-key pushed via Management API; Vercel env flipped via REST + redeploy; local .env + CLI link now point at ntrg. Teardown checklist (rollback window ~1-2 weeks) lives in the runbook: delete old provider keys, dry-run project `oxzteqqrhggdodcnngzn`, pause-then-delete old project, re-reset all DB passwords, revoke the two session tokens, delete C:\Users\jonas\supabase-migration, raise ntrg global storage upload limit + re-add 100MB caps on project-files/scan-data. **Session rule change same day: sonnet subagents BANNED repo-wide (die on context overflow; 2nd occurrence 2026-08-17) — all delegation now opus-only; codified in root CLAUDE.md, .claude/CLAUDE.md, settings.json hook.**
- [[security-audit-2026-08-12]] - **Security/GDPR audit remediation IMPLEMENTED 2026-08-12/13** (branch `worktree-security-audit-remediation`, worktree off master `ce47482`, uncommitted→committed there; NOT yet merged/deployed): both Criticals (C1 gemini-proxy → true relay, key never leaves the server; C2 `manager` org-scoped at table AND storage layers + NULL-safe/`anon`-revoked permission RPCs), all High (H1 controlled-docs org scope via `controlled_document_org()` definer helper; H2 annotations bucket private+org-scoped+signed URLs; H3 label XSS → DOM APIs + `isSafeImageUrl`; H4 erasure now removes Storage; H5 org_admin history/comments scoped; H6 npm 9H+6M→1H+4M), and the code-addressable Mediums/Lows (M1 vercel.json CSP/HSTS, M3/M4 role-rank in 5 edge fns via `_shared/role-rank.ts`, M5 send-email constraints, M6/M7 erasure/export completeness + fail-loud, M9 enumeration oracle, M11 partial — avatars public flag deferred (app uses `getPublicUrl`), L2-L8). Five new migrations `20260812120000..124000`. **Process:** 8 opus implementers + 2 adversarial reviewers — both reviews returned do-not-merge with real catches (phantom `notification_emails` table would have bricked every deletion under the new fail-loud contract; profiles policy re-introduced the RLS-recursion scar shape; NULL-role + `EXECUTE TO PUBLIC` anon self-promotion chain in `approve_permission_request`; nested-RLS status-gate regression in the H1 policy) — then 2 fixup agents closed all blockers/majors. Gate: build ✓ 2m1s, vitest exit 0 (65.1% stmts), lint 0 errors, tsc clean. **All sonnet-tier subagents died instantly with "Prompt is too long" this session — retier to opus if it recurs.** BEFORE DEPLOY (full checklist = audit doc appendix): rotate `GEMINI_API_KEY` + deploy 12 edge fns; confirm `manager` = customer-facing decision + no NULL-org accounts; dump live `pg_policies` (profiles shape, `20260629120000` applied?); migration `20260812124000` must ship WITH the frontend. Pre-existing discoveries: org_admin permission-approvals likely already broken in prod (role-UPDATE vs profiles trigger); orphaned-admin sync trap; possible legacy PDFs in public avatars bucket. **UPDATE 2026-08-24: FULLY DEPLOYED.** Merged to master `9f069a9`, pushed both remotes; Vercel auto-deployed the frontend (CSP/HSTS headers verified live via curl). Same day: all 12 edge functions deployed to `ntrgjqrbewbvwofupphn` (`--use-api`; serve-client-share untouched to preserve its no-JWT config) and the 5 migrations applied (`db push --include-all` — required because the 0812 timestamps sort before the already-applied 0820/0821 client-share migrations; ledger verified local==remote). gemini-proxy 401 gate probed ✓; vessel-annotations public-read probe 400 ✓; avatars deliberately still public (M11 deferral). Owner kept the existing GEMINI key (rotated 2026-08-17 with the migration; audit's assume-compromised stance noted and declined) and pushed migrations without the NULL-org-manager pre-check — if a manager account suddenly sees nobody, assign it an organization. Scars: the desktop `.env` carried a UTF-8 BOM (PS5.1 Out-File artifact) that made every supabase CLI call fail with "unexpected character '»'" — strip the BOM and never write `.env` via Out-File; the CLI stores its token in the Windows credential store, so arbitrary pre-flight SQL needs the dashboard editor on this machine. Still open from the checklist: dashboard SQL checks (NULL-org managers, profiles SELECT shape, client-shares anon grants), role-based smoke tests, npm advisory major bumps, the two missing 2FA edge fns, avatars signed-URL refactor.**
- [[plans/2026-08-10-coverage-dome-stats-drape-fix-design]] - **Coverage stats dome-end fix (2026-08-10, uncommitted on `feature/appendage-bodies`):** head-touching coverage rects were DRAWN as rigid geodesic drapes but MEASURED as axial-pos × equator-angle boxes, so a rect visually covering most of a head reported ~25% (double under-count: meridian overhang read as axial z, angle span frozen at equator). New pure `engine/head-coverage.ts` rasterizes each head in meridian-arc (s,θ) space with ~square physical cells and splats the SAME `buildDrapeGrid` the visual uses (polyline sub-stepped, overlap-aware union, exact r·dθ·ds cell areas); `computeCoverage` routes rects via scan-sampling's `rectIsPureCylinder` (param generalized to a structural subset — the ONE geometry predicate, never re-derived), head-rect boxes clamp to [0,L] for the shell sweep (drape ≡ box on the cylinder), pure-cylinder models byte-identical. **Clairaut lesson (baked into tests — do not "fix" back):** a tangent-centred full-wrap rect GENUINELY covers only ~50% of a head — drape lateral edges are geodesics and bounce away from the pole (r·sinψ = const), same in stats and the drawn drape; the 100% full-cover oracle is a POLE-CENTRED rect (verified 100.0%). Gate: engine suite 759 passed | 3 skipped, build ✓ (2m2s), lint 0 errors (only pre-existing max-lines warnings). **Incident, same session:** a concurrent Claude session's main loop (handling "merge with the live branch") ran `git stash` + `checkout master` + started `merge feature/appendage-bodies` in the shared working tree, leaving a conflicted half-merged tree that broke the running dev app (piping gone, old visuals); recovered with `merge --abort` + branch checkout + `stash pop`, zero work lost. That session then redid the merge safely in an isolated `git worktree` (landed as master `5823e5f`, pushed to GitHub). Standing rules added to `.claude/CLAUDE.md`: subagents never run git state-changing commands, and shared-tree branch switches/merges are forbidden while another session may be active — use an isolated worktree.
- [[plans/2026-08-06-t3-ux-batch-design]] - **ALL FIVE T3 PHASES LANDED 2026-08-06/07** (owner confirmed perf good after `50def17` first): A3 labeled undo + history dropdown (`2e4bd39` — HistoryEntry {vessel,label,at}, dispatcher-minted labels, folded UNDO_TO/REDO_TO jumps), C12 view cube/canonical views/camera bookmarks (`8685357` — canonical-views.ts, CSS-3D ViewCube rAF-synced, cameraBookmarks top-level field both paths + report-capture feed), C13a uniform entity visibility (`d9f5046` — visible? on all types, byte-identical hash projections, tier-2 in-place apply, raycast exclusion, stats untouched) + C13b outliner panel (`509bd62` — pure outliner-tree + OutlinerPanel, Eye toggles via D1 hooks), C14 command palette + **view-cube positioning fix** (Ctrl+K palette-registry/frame-entity/CommandPalette; the cube was mounted top-LEFT under the absolute 340px `.vm-sidebar` overlay and never visible — root-caused via Playwright drive, moved top-RIGHT, outliner made sidebar-aware; new viewport-overlay rule in Module Map), C15 clip planes (clip-planes.ts + scene-wide setClippingPlanes + rebuild re-application, ClipPlanesControl, runtime-verified GPU cut). Both C14 halves + C15 were runtime-verified in a headless-Edge Playwright drive (temp /verify-modeler route, reverted). Final gate: build ✓, suite 1549 passed | 3 skipped (useLayoutMode OOM flake only). Note: the four research subagents crashed on context overflow this arc — implementation agents ran fine with explicit chunked-read instructions in their prompts. Deferred: annotations/images visible→tier-2 unification (CSS2D label lifecycle), CSS2D labels don't clip, clipped geometry still raycastable.
- [[plans/2026-08-05-boot-parity-feedback-batch-design]] - **ALL FIVE ITEMS LANDED 2026-08-05/06** from the owner's hands-on review: R1 nozzle-bore stat cutouts (`050e0a1` — `engine/nozzle-footprint.ts`, radial exact / non-radial projected ellipse, per-body incl. boots, live memo-dep fix; deferred: head-mounted nozzles, boot-composite visual holes), R5 vertical-vessel 2D orientation (`50771d1` — FlattenedView never read `orientation`; `makeDevelopedFrame()` is now THE single (axial,circ)↔canvas mapping, vertical = pure transpose portrait, horizontal byte-identical), R3 user-facing "Boot" terminology (`9124a86` — strings only, identifiers/serialization untouched, persisted names keep their saved values), R4 relief view → **Topo viewport mode** (`2bab5fc` — 3D/2D/Topo stack, `ReliefViewportPane.tsx` overlay toolbar, `ReliefViewModal` deleted, viewMode transient/unserialized), R2 cursor-first cross-body mounting (`c098a15` — `engine/body-crossing.ts` 6mm seam hysteresis + frame-conversion invariant; drags/draws/drops raycast all bodies, Mount-on selects → `MountedOnChip`; excluded: dome scans, textures/saddles, pipe-part drops; confirmed composites have no gizmo → remount = re-enter orientation). Suite 1041→~1394. Next: T3 UX batch (C12 view cube/bookmarks, C13 outliner, C14 command palette, C15 clip planes).
- [[plans/2026-07-30-vessel-modeler-ux-roadmap]] - **Tranche 1 COMPLETE 2026-07-31** (`3d099a5` cutouts, `80401d1` per-body stats, `32f23db` unified sampler/A1, `1467e60` welds+lugs+rects, `b94c447` flattened footprints+strips, `7870af1` dome-on-end, `9a2d60b` annotations): full appendage feature parity — junction openings excluded from stats and clipped in overlays via one predicate; wall-loss/coverage per body with Combined default; every attachable mounts on appendages with spec-declared bodyId, cascade cleanup, byte-identical legacy goldens throughout. Punch list still open: weld-label placement on appendage welds; VesselModeler.tsx integrations deferred while the GA session holds the file (draw-on-appendage context, direct dome import to a closure, import-modal section label); 2D welds/lugs/dome-scans on appendage strips; if dome scans ever upsert to `scan_composites` keyed by section, disambiguate to `appendage:<id>:end`. Next tranche: T2 foundations (vessel-coords.ts, VesselModeler decomposition, stable nozzle ids).
- [[plans/2026-07-30-ga-drawing-import-hardening-design]] - IMPLEMENTED same day (all 5 phases incl. Addendum, uncommitted on `feature/appendage-bodies`). Addendum from BRT1-VA1 field test: **head-mounted nozzles** — extraction schema gained per-nozzle `mount` (shell/head-left/head-right) + `radialOffset` (mm from CL); `engine/head-nozzle-placement.ts` maps head mounts onto the ellipsoid (pos = ∓headDepth·√(1−(2r/id)²) from the respective tangent line, axial orientation via `orientationMode:'horizontal'` + azimuth 90/270 resolved from clock angle sign — convention per plans/2026-06-19 nozzle-vertical-axis-rotation); prompt/voting/verifier extended (offset < id/2 rule; shell sentinel never gates). **Compact review** — `DrawingReviewPanel` rewritten as a dense grid (`DrawingReviewRows.tsx`): one row per nozzle/saddle, quiet-by-default tints (border-only tokens: warning=missing, danger=low/flagged, success=edited), "X of Y read cleanly" header + issues-only filter, mount select switches the position column between pos and offset-from-CL, mount/offset now gate apply. Field test 2 (uniform 90° feature rotation) → Phase F: angles are now drawing-native end-to-end (0=TDC clockwise, model transcribes labels only); the single conversion `drawingClockToVesselAngle` lives in head-nozzle-placement.ts at apply time (flattened-view TDC lesson applied — no model-side or UI-side conversion, ever); review gains a Rotate-all ±90/180/Mirror escape hatch. Field test 3 (zero nozzles) → prompt restored toward proven-good wording (geometric angle estimation permitted, names pinned to TAGs) + voting never silently discards singleton nozzles (1-of-3 → low + 'single-pass' flag). Field test 4 → Phase G horizontal nozzles: `nozzleOrientation` required classification + `elevation` (EL from CL); placement derives shell angle asin(2EL/id) per facing side and corrects projection for the center-plane convention (proj_engine = proj_drawing + R − √(R²−EL²); engine consumes proj as axis-referenced stick-out). Also fixed post-launch: the vestigial eager `reviewToLenientResult` call threw on missing vessel ID before the review screen could open — extraction now always delivers the review and strictness lives only at apply. Original phases: GA→3D import hardened after research audit (crop→VLM shape validated against production drawing-digitization practice; the gap was the single unverified pass). Extraction now runs a 3-call Gemini ensemble (temp 0.7, seeds 41-43, enforced `responseSchema`, JSON.parse only) reconciled per-field by pure voting (`engine/drawing-extraction-voting.ts`, agreement→high/medium/low/missing confidence) + rules verifier (`engine/drawing-verifier.ts`: plausibility ranges, standard-NPS flag-never-snap, duplicate/unmatched tags). **Silent defaulting deleted** — unreadable fields are `missing` and block apply until user-edited in the new editable `DrawingReviewPanel` (confidence badges, per-field edit, nozzle/saddle removal, PDF page selector). Apply path: `pipelines[].nozzleIndex` remapped by nozzle name via `engine/nozzle-ref-remap.ts` (unmatched → user-confirmed removal, never silent), proj/size/saddle-pos clamps added to `validateVesselState`. Dead `RegionSelector.tsx`/`DrawingResultView.tsx` deleted. 45 new unit tests + 1 RTL test. Model stays Gemini Flash tier via `MODEL_CANDIDATES` fallback list (verify newer Flash IDs against ListModels before prepending). Deferred: cross-model second opinion, CV leader-line pre-pass, provenance persistence.
- [[plans/2026-07-30-vessel-modeler-ux-roadmap]] - approved vessel-modeler backlog (2026-07-30): T1 appendage completion (finish Phase 3 + unified dome/body-aware sampler + Phase 4 attachable parity) → T2 foundations (vessel-coords.ts, VesselModeler decomposition, structural-hash fix, stable nozzle ids) → T3 UX batch (undo labels, view cube/bookmarks, outliner, command palette, clip planes); TopologyViewer absorb recommended (pending), CML layer (B6) parked. Verified 2026-07-30: dome-aware annotation stats/heatmap NOT implemented (only geometry drapes are — sampling still pure-cylinder; annotation-heatmap still holds the comment-synced duplicate sampler); appendage Phase 3 stalled after T1 (junction-footprint.ts had zero callers). T0 branch-hygiene commits landed 2026-07-30 (five interleaved workstreams committed in attributed chunks).
- [[plans/2026-07-29-vessel-modeler-undo-redo-design]] - IMPLEMENTED same day (all 3 phases, uncommitted on `feature/appendage-bodies`): snapshot undo/redo over the immutable `vesselReducer`, vessel slice only. New `engine/vessel-history.ts` (pure past/future stacks, cap 50, coalesce same-key <1s; 12 unit tests) + `useTextureRehydration.ts` (rebuilds disposed THREE textures from `imageData` on undo — must store into a NEW `textureObjectsRef` object, ThreeViewport detects texture changes by identity). Domain wrappers auto-derive coalesce keys (`entity:id:sortedFields`) so drag storms AND per-keystroke sidebar edits collapse to one entry; `onDragEnd` dispatches `HISTORY_BREAK`; the 4 cloud-rehydration dispatches are `skip`-tagged; `SET_VESSEL` clears history (load = boundary). UNDO/REDO reset selection/draw/previews and re-derive `ui.labelsTidied`; `modelMode` syncs from `vesselShape` via effect (mode toggle guarded against redundant-click spurious entries). Ctrl+Z/Ctrl+Shift+Z/Ctrl+Y with text-input guard + toolbar buttons. Verified: build/typecheck/lint clean, 12/12 + 61 serialization/structural-hash tests, in-place-mutation audit CLEAN, 9-scenario headless Playwright drive all PASS. Known pre-existing failures NOT from this work: `personnel-service.test.ts` assertion (parallel-session personnel changes), `useLayoutMode.test.ts` OOM flake.
- [[plans/2026-07-28-competency-attribution-and-session-hardening-design]] - incident 2026-07-28: an admin filed Ben Wilkes' TOFD L2 cert under Richard Biggar's profile (human error — RLS worked as designed; full 3-agent forensic sweep confirmed no policy, session, or edge-function defect). Response feature: `employee_competencies.created_by` (tamper-proof BEFORE INSERT trigger, FK → profiles for name embeds; migration `20260728130000`), `audit_row_change()` now records `details.on_behalf_of` when the actor writes a row owned by someone else (INSERT details were previously NULL — actor was logged but not target), ActivityLogTab "for {name}" + on-behalf filter, "Added by {name}" on competency cards, storage INSERT policy gains `super_admin` (`20260728131000`, was admin-only — under-permission). Session hardening from the same investigation: header now shows logged-in name+role (LayoutNew previously rendered NO identity), restored-session "Not you?" banner, auth-supabase stale-identity guard fixed (`session.user.id !== currentUser?.id`, was `!this.currentUser`), `assertActiveUser` guard on ProfilePage self-service writes, AUTH-DEBUG PII logs stripped. No-op auth lock rework deliberately deferred (see Decision Log). Historic forensics: correlate `activity_log` actor (action_type `competency_created`) with the row's `created_at`. **Both migrations applied to live Supabase 2026-07-29** (`db push`, verified via `migration list`) — server-side attribution capture is live for all inserts including from the old deployed frontend; the UI surfacing ships with the next frontend deploy (code uncommitted as of push).
- Admin create-user 400 fix (2026-07-28): edge-function error bodies were unreadable client-side — supabase-js v2 `FunctionsHttpError.context` is a raw `Response`, so `context?.error` is always undefined; `auth-users.ts` now parses `context.json().error` (`extractInvokeError` + regression test). `CreateUserModal` now validates via shared `validatePasswordStrength` (gate on `feedback.length` — `isValid` does NOT cover the user-info/common-password checks), mirroring the server-only rejections that caused opaque 400s. `create-user` edge fn: `manager` added to VALID_ROLES, `super_admin` creation gated to super_admin actors, invalid roles are explicit 400s instead of silent viewer downgrade — edge fn deployed to prod same day (CLI 2.107.0, smoke-tested live).
- [[plans/2026-07-28-competency-multi-documents-design]] - certifications now support multiple documents/pages: child table `competency_documents` (+ RLS mirroring parent, backfill) with parent scalars mirrored to first doc; multi-file upload field, paged viewers in profile card + both review modals; migration `20260728120000` should be applied to live Supabase before/with the frontend deploy; reads are self-healing regardless (PGRST200 → `withCompetencyDocumentsFallback` retries once without the embed, legacy single-doc mode until reload — added same day after the personnel page hard-failed pre-migration)
- [[plans/2026-07-24-annotation-dome-mapping-design]] - annotations now map onto dome ends in meridian arc-length space (`engine/dome-arc.ts`), true mm footprint, seamless shell-dome crossing; addendum same day: apex reachability (radiusAt clamp 0.99 -> 1.0, the 0.141R untouchable disk at dome centres is gone); stats/heatmap dome-awareness is a recorded follow-up
- [[plans/2026-07-21-cscan-mm-less-filename-arbitration-gap]] - BRT V-1001 root cause + arbitration v2 (bare tokens, Data File source, metadata halving, localZero anchor, overlap cross-check, truncation warnings)
- [[plans/2026-07-12-tools-projects-comprehensive-review]] - verified review + pivot analysis of Tools/Projects (18 claims adversarially verified; roadmap Phases 0-4)
- [[plans/2026-06-11-cscan-offset-filename-arbitration]]
- [[plans/2026-05-01-industrial-theme-handover]]
- [[plans/2026-04-30-industrial-instrument-handover]]
- [[plans/2026-04-23-vessel-overview-page-implementation]]
- [[plans/2026-04-23-scan-viewer-landing-implementation]]
- [[plans/2026-04-16-paut-report-pdf-build-plan]]

## Working Rhythm

- Capture rough ideas in [[inbox/README|Inbox]].
- Promote implementation plans to `docs/plans/YYYY-MM-DD-topic.md`.
- Capture lasting architectural choices with [[templates/Decision Record|Decision Record]].
- End substantial work with [[templates/Handover Note|Handover Note]] so the next session has a clean trail.
- For agent continuity, update [[Agent Memory]] only when a change affects future project orientation.

## Useful Code Landmarks

- App routing: `src/App.tsx`
- Vessel modeler: `src/components/VesselModeler/`
- Vessel model services: `src/services/vessel-model-service.ts`
- Project workflow: `src/pages/projects/`, `src/components/projects/`
- Scan viewer: `src/components/projects/scan-viewer/`
- C-scan visualizer: `src/components/CscanVisualizer/`
- Supabase client: `src/supabase-client.ts`
- Database scripts: `database/`, `supabase/migrations/`
